Podemos definir um container (o qual iremos nos referir por BOX), que conterá então campos. Inicialmente, apenas um. Deve conter, também, três operações básicas:

-   armazenar,
-   recuperar,
-   alterar.
